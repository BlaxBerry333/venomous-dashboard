# Use a Go version compatible with the host (1.23.0)
FROM golang:1.23.0

# Set the working directory
WORKDIR /app

# Copy module files first to leverage Docker cache.
# This layer only gets rebuilt if go.mod or go.sum change.
COPY go.mod go.sum ./

# Download dependencies.
RUN go mod download

# Copy the rest of the source code
COPY . .

# Expose the port the app runs on
EXPOSE 8080

# Run the app
CMD ["go", "run", "main.go"]
