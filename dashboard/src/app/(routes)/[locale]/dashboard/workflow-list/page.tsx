import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Workflow List",
  description: "...",
};

export default async function DashboardWorkflowListPage() {
  return <div>Workflow List</div>;
}
