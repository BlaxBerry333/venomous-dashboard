import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Media Detail",
  description: "...",
};

export default async function DashboardMediaDetailPage() {
  return <div>Media Detail</div>;
}
