import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Workflow Detail",
  description: "...",
};

export default async function DashboardWorkflowDetailPage() {
  return <div>Workflow Detail</div>;
}
