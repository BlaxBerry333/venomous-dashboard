import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Media List",
  description: "...",
};

export default async function DashboardMediaListPage() {
  return <div>Media List</div>;
}
