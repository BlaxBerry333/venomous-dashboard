import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Workflow Create",
  description: "...",
};

export default async function DashboardWorkflowCreatePage() {
  return <div>Workflow Create</div>;
}
