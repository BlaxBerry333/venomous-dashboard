import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Notes Detail",
  description: "...",
};

export default async function DashboardNotesDetailPage() {
  return <div>Notes Detail</div>;
}
