import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Notes Create",
  description: "...",
};

export default async function DashboardNotesCreatePage() {
  return <div>Notes Create</div>;
}
