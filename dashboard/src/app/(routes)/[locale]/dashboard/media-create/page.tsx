import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Media Create",
  description: "...",
};

export default async function DashboardMediaCreatePage() {
  return <div>Media Create</div>;
}
